export interface User {
    _id?: string;
    dob: string;
    email_address: string;
    created_date: string;
    modified_date: string;
    first_name: string;
    last_name: string;
    _password: string;
}

export interface Signup {
    
}